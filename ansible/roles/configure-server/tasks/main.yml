---
- name: Download AWS CLI
  get_url:
    url: "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip"
    dest: "/tmp/awscli.zip"

- name: "Unzip downloaded AWS CLI"
  shell: unzip awscliv.zip
  args:
    chdir: $HOME

- name: "Install AWS CLI"
  shell: ./aws/install
  args:
    chdir: $HOME